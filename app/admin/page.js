import {createClient} from "../../lib/supabase/server";import {createAdminClient} from "../../lib/supabase/admin";import {redirect} from "next/navigation";
async function requireAdmin(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const {data}=await s.from("admins").select("user_id").eq("user_id",user.id).maybeSingle();if(!data)redirect("/");return user}
async function addProduct(fd){"use server";await requireAdmin();const db=createAdminClient();await db.from("products").insert({name:String(fd.get("name")),description:String(fd.get("description")||""),category:String(fd.get("category")||""),image_url:String(fd.get("image_url")||""),price:Number(fd.get("price")),cost:Number(fd.get("cost")),supplier_url:String(fd.get("supplier_url")||""),active:true});redirect("/admin")}
async function logout(){"use server";const s=await createClient();await s.auth.signOut();redirect("/login")}
export default async function Admin(){await requireAdmin();const db=createAdminClient();const [{data:products},{data:orders}]=await Promise.all([db.from("products").select("*").order("created_at",{ascending:false}),db.from("orders").select("*").order("created_at",{ascending:false})]);const revenue=(orders||[]).reduce((s,o)=>s+Number(o.amount_total||0),0)/100;return <main className="wrap"><div className="sectionHead"><div><p className="eyebrow">PRIVATE ADMIN</p><h1>Southstar dashboard</h1></div><form action={logout}><button>Sign out</button></form></div><div className="stats"><div>ORDERS<b>{orders?.length||0}</b></div><div>REVENUE<b>${revenue.toFixed(2)}</b></div><div>PRODUCTS<b>{products?.length||0}</b></div></div><section className="panel"><h2>Add product</h2><form action={addProduct} className="form"><input name="name" placeholder="Product name" required/><textarea name="description" placeholder="Description"/><input name="category" placeholder="Category"/><input name="image_url" placeholder="Image URL"/><input name="price" type="number" step="0.01" placeholder="Selling price" required/><input name="cost" type="number" step="0.01" placeholder="Supplier cost" required/><input name="supplier_url" placeholder="Private supplier URL"/><button className="primary">Add product</button></form></section><section className="panel"><h2>Orders</h2>{orders?.length?orders.map(o=><div className="order" key={o.id}><b>${(Number(o.amount_total)/100).toFixed(2)} • {o.payment_status}</b><p>{o.customer_name} • {o.customer_email}</p><p>{o.shipping_address}</p></div>):<p>No orders yet.</p>}</section></main>}
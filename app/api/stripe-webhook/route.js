import Stripe from "stripe";import {headers} from "next/headers";import {createAdminClient} from "../../../lib/supabase/admin";
export async function POST(req){const stripe=new Stripe(process.env.STRIPE_SECRET_KEY);const body=await req.text(),h=await headers();let event;try{event=stripe.webhooks.constructEvent(body,h.get("stripe-signature"),process.env.STRIPE_WEBHOOK_SECRET)}catch(e){return new Response(`Webhook error: ${e.message}`,{status:400})}if(event.type==="checkout.session.completed"){const s=event.data.object,db=createAdminClient(),li=await stripe.checkout.sessions.listLineItems(s.id,{limit:100}),a=s.shipping_details?.address;await db.from("orders").upsert({stripe_session_id:s.id,amount_total:s.amount_total,payment_status:s.payment_status,customer_email:s.customer_details?.email||"",customer_name:s.shipping_details?.name||s.customer_details?.name||"",shipping_address:a?[a.line1,a.line2,a.city,a.state,a.postal_code,a.country].filter(Boolean).join(", "):"",items:li.data.map(x=>({description:x.description,quantity:x.quantity,amount_total:x.amount_total}))},{onConflict:"stripe_session_id"})}return Response.json({received:true})}
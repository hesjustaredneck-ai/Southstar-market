import {createClient} from "../../lib/supabase/server";import {redirect} from "next/navigation";
async function login(fd){"use server";const s=await createClient();const {error}=await s.auth.signInWithPassword({email:String(fd.get("email")||""),password:String(fd.get("password")||"")});if(error)redirect("/login?error=1");redirect("/admin")}
export default async function Login({searchParams}){const q=await searchParams;return <main className="wrap"><p className="eyebrow">PRIVATE AREA</p><h1>Admin login</h1>{q?.error&&<p className="notice">Login failed.</p>}<form action={login} className="form panel"><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required/><button className="primary">Sign in</button></form></main>}